events:
  # /!\ be careful when removing an event
  test.event:
    secret: ${TEST_SECRET}
  decapode.resource.modified:
    secret: ${DECAPODE_SECRET}
  datagouvfr.dataset.created:
    secret: ${DATAGOUVFR_SECRET}
  datagouvfr.dataset.discussed:
    secret: ${DATAGOUVFR_SECRET}
